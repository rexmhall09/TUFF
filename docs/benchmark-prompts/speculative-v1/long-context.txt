Summarize the following notes in one concise paragraph, preserving the causal order. The project began as a small tokenizer experiment. The first profiling pass found that model weights, rather than tokenization, dominated runtime. A later pass added chunked prefill and reduced command-buffer overhead. The remaining question is whether several proposed decode tokens can share the cost of streamed expert weights. The experiment must compare a resident control with an SSD-streamed mixture-of-experts model, repeat each condition, and report both tokens per second and storage traffic per emitted token. The conclusion should distinguish measured facts from hypotheses.
